types:
  config_free_dialog_group:
    seq:
      - id: base
        type: config_dialog_group
    instances:
      # Base class fields
      talk_id:
        value: base.talk_id
      type:
        value: base.type
      dialog_list:
        value: base.dialog_list
